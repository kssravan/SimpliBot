import { StyleSheet } from 'react-native';
export default StyleSheet.create({
    button: {
        width:'100%',
        marginTop: 30,
    },
    signIn: {
        height: 50,
        justifyContent: 'center',
        alignItems: 'center',
        borderRadius: 10,
        flexDirection: 'row',
    },
    signInText: {
        color: 'white',
        fontWeight: 'bold',
    }
});